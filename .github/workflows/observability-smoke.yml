name: Observability Smoke

on:
  workflow_dispatch:

jobs:
  smoke-observability:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: chmod +x ./scripts/smoke-observability.sh && ./scripts/smoke-observability.sh
